# 캡스톤디자인2 Spatial Archive 배포 가이드

이 문서는 `캡스톤디자인2포트폴리오.html`을 다른 학부생들과 공유할 수 있는 공개 웹페이지로 배포하는 방법을 설명합니다. 가장 간단한 선택지는 **GitHub Pages**이며, 자동 배포와 프리뷰 URL이 필요하면 **Vercel**을 사용하면 됩니다.

## 1. 배포 전 파일 구조

같은 폴더에 다음 두 파일을 둡니다.

```text
capstone-portfolio/
├─ 캡스톤디자인2포트폴리오.html
└─ portfolio-hero.jpg
```

HTML 안의 히어로 이미지 경로가 `portfolio-hero.jpg`로 설정되어 있으므로, 이미지 파일을 HTML과 같은 폴더에 두어야 합니다. 파일명과 대소문자를 바꾸면 Linux 기반 호스팅에서 이미지가 보이지 않을 수 있습니다.

## 2. GitHub Pages로 배포하기

### 2.1 저장소 만들기

1. GitHub에 로그인합니다.
2. 우측 상단의 **New repository**를 선택합니다.
3. 저장소 이름을 `capstone-design-2-portfolio`처럼 지정합니다.
4. 다른 학부생에게 공유할 목적이면 저장소를 **Public**으로 설정합니다.
5. `Add a README file`은 선택 사항입니다.

### 2.2 파일 업로드

터미널을 사용한다면 다음 명령을 실행합니다. `YOUR_ID`는 본인의 GitHub 사용자명으로 바꿉니다.

```bash
git clone https://github.com/YOUR_ID/capstone-design-2-portfolio.git
cd capstone-design-2-portfolio
cp /path/to/캡스톤디자인2포트폴리오.html .
cp /path/to/portfolio-hero.jpg .
git add .
git commit -m "Create capstone architecture portfolio"
git push origin main
```

웹 브라우저에서 업로드하는 경우에는 저장소의 **Add file → Upload files**를 선택한 후 두 파일을 동시에 업로드하고 **Commit changes**를 누르면 됩니다.

### 2.3 Pages 활성화

1. 저장소의 **Settings → Pages**로 이동합니다.
2. **Build and deployment**에서 `Deploy from a branch`를 선택합니다.
3. Branch는 `main`, 폴더는 `/ (root)`를 선택하고 **Save**를 누릅니다.
4. 잠시 후 다음 주소에서 접속합니다.

```text
https://YOUR_ID.github.io/capstone-design-2-portfolio/
```

`index.html`이 없기 때문에 주소 뒤에 HTML 파일명을 붙여야 하는 설정이 될 수 있습니다. 가장 깔끔한 주소를 원하면 파일명을 `index.html`로 변경합니다.

```bash
mv 캡스톤디자인2포트폴리오.html index.html
git add .
git commit -m "Use index entry point for GitHub Pages"
git push origin main
```

그 후 주소는 다음처럼 사용할 수 있습니다.

```text
https://YOUR_ID.github.io/capstone-design-2-portfolio/
```

### 2.4 다른 학생에게 공유하기

GitHub Pages 주소를 카카오톡, LMS, 이메일 등에 그대로 공유하면 됩니다. 특정 학생의 상세 화면은 다음과 같이 해시 주소를 공유할 수 있습니다.

```text
https://YOUR_ID.github.io/capstone-design-2-portfolio/#/s/s1
```

현재 샘플 데이터는 `s1`, `s2` 상세 화면에서 바로 확인할 수 있습니다.

## 3. Vercel로 배포하기

### 3.1 GitHub 연동 방식

1. [Vercel](https://vercel.com)에 GitHub 계정으로 로그인합니다.
2. **Add New → Project**를 선택합니다.
3. 방금 만든 GitHub 저장소를 Import합니다.
4. Framework Preset은 **Other** 또는 **No Framework**로 둡니다.
5. Build Command는 비워 두고, Output Directory도 비워 둡니다.
6. **Deploy**를 누릅니다.

Vercel이 발급하는 `*.vercel.app` 주소를 다른 학부생에게 공유하면 됩니다. 이후 GitHub의 `main` 브랜치에 push할 때마다 자동으로 다시 배포됩니다.

### 3.2 Vercel CLI 방식

Node.js가 설치되어 있다면 다음 방식으로도 배포할 수 있습니다.

```bash
npm i -g vercel
cd capstone-portfolio
vercel login
vercel
```

질문이 나오면 다음처럼 선택합니다.

```text
Set up and deploy? Y
Which scope? 본인 계정
Link to existing project? N
What’s your project’s name? capstone-design-2-portfolio
In which directory is your code located? ./
Want to modify these settings? N
```

운영 배포는 다음 명령으로 진행합니다.

```bash
vercel --prod
```

## 4. 현재 페이지의 데이터 동작과 운영 전환

현재 HTML은 두 가지 모드로 동작합니다.

| 환경 | 학생 목록 | 상세 기록 | 업로드 저장 | AI 리뷰 |
|---|---|---|---|---|
| GitHub Pages/Vercel 정적 배포 | 기본 15명 | `s1`, `s2`에 실제 설계 프로세스형 샘플 데이터 표시 | 브라우저 새로고침 후 유지되지 않음 | 연결된 API가 없으면 기존 리뷰 표시 |
| `window.claude`가 제공되는 호스트 환경 | DB의 학생 컬렉션 | DB의 uploads 컬렉션 | Assets API로 업로드 | Sample API로 생성 |

정적 호스팅은 HTML과 이미지 파일만 제공하므로, 학생들이 올린 이미지와 기록을 여러 기기에서 공유하려면 별도 백엔드가 필요합니다. 운영 전환 시에는 다음 구조를 권장합니다.

1. **학생 정보**: `students/{studentId}` 컬렉션
2. **설계 기록**: `uploads/{uploadId}` 컬렉션
3. **이미지**: S3, Cloudinary, Firebase Storage 등 객체 저장소
4. **AI 리뷰**: 서버 API에서 API 키를 보관하고, 브라우저에는 키를 노출하지 않도록 처리
5. **접근 제어**: 공개 읽기와 관리자 업로드 권한을 분리

현재 샘플 데이터는 단순한 이름·주차 숫자가 아니라, 실제 설계 스튜디오에서 검토하는 **대상지 리서치, 면적, 프로그램, 동선, 단면, 채광, 재료, 환기, 접근성** 항목을 포함합니다. 다만 학생의 실제 제출물로 오인되지 않도록, 운영 공개 전에는 학생명과 설계안 설명을 실제 팀 데이터로 교체해야 합니다.

### AI Review Lens 분석 알고리즘

학생 상세 화면의 `AI Review Lens`는 생성형 AI의 서술형 코멘트를 대체하지 않고, 설계 설명과 `specs`에 포함된 근거를 빠르게 구조화하는 **사전 분석 루브릭**입니다. 제목·설명·설계 사양을 하나의 텍스트로 결합한 뒤 건축 설계 검토에 필요한 키워드와 수치 표현을 탐지합니다. 각 항목의 기본 점수는 44점이며, 관련 근거가 탐지될 때마다 10점씩 가산하고 설명이 180자를 넘으면 8점을 추가합니다. 항목별 점수는 96점을 상한으로 제한합니다.

| 지표 | 가중치 | 탐지하는 근거 예시 | 해석 |
|---|---:|---|---|
| 공간 구성 | 25% | 대지, 면적, 프로그램, 배치, 공용공간 | 대상지·프로그램·공간 조직의 명료성 |
| 동선·무장애 | 20% | 동선, 출입, 반입, 회전, 폭, 휠체어, 접근 | 사용자 흐름과 접근성 검토 수준 |
| 구조·시공 | 20% | 구조, 기둥, 철골, 단면, 접합, 프레임, 치수, 모듈 | 구조·시공 논리와 디테일 근거 |
| 환경·재료 | 20% | 채광, 일사, 환기, 흡음, 재료, 에너지, 저VOC | 환경 성능과 재료 전략의 구체성 |
| 검증 근거 | 15% | ㎡, m, %, 1:, 수치, 기준, 피크타임, 인증 | 설명을 검증 가능한 수치·기준으로 연결하는 정도 |

종합 점수는 위 다섯 항목의 가중 평균입니다. 학생 전체 대시보드는 모든 기록의 평균 점수를 표시하고, 가장 낮은 항목을 `약점 지표`로 지정합니다. 65점 미만은 **높음**, 65–77점은 **중간**, 78점 이상은 **낮음** 우선순위로 표시합니다. 각 주차 카드에는 항목별 점수, 탐지된 근거 건수, 가장 보강이 필요한 지표가 함께 나타납니다. 이 점수는 자동 판정이나 성적이 아니라, 지도교수가 다음 크리틱에서 확인할 도면·치수·시뮬레이션 항목을 빠르게 찾기 위한 보조 지표입니다.

## 5. 배포 후 점검 목록

- 홈 화면에서 건축 모형 히어로 이미지가 보이는지 확인합니다.
- `#/s/s1`에서 4개 주차 기록과 AI Design Review 패널이 보이는지 확인합니다.
- `#/s/s2`에서 커뮤니티 키친 설계 기록이 보이는지 확인합니다.
- 학생 상세 화면 상단의 AI Review Lens에서 종합 점수·5개 지표·가중치 설명을 확인합니다.
- 각 주차 카드에서 항목별 점수와 `NEXT` 우선순위가 설계 설명의 근거와 일치하는지 확인합니다.
- 모바일 화면에서 학생 카드가 1열로 바뀌는지 확인합니다.
- 브라우저 새로고침 후에도 정적 샘플 데이터가 다시 표시되는지 확인합니다.
- 학생 업로드를 실제 운영에 사용할 경우 정적 호스팅만으로는 영구 저장되지 않는다는 점을 공유합니다.

## 6. 가장 빠른 공유 방법

실습이나 발표 당일에는 다음 순서가 가장 빠릅니다.

```bash
# GitHub 저장소를 한 번 만든 뒤
cd capstone-portfolio
git add .
git commit -m "Update architecture portfolio"
git push origin main
```

GitHub Pages는 push 이후 자동으로 갱신되고, Vercel은 보통 별도의 설정 없이 새 배포를 생성합니다. 발표 자료에는 홈 URL과 함께 `#/s/s1` 또는 실제 학생 ID에 해당하는 상세 URL을 함께 넣으면 학생별 화면을 바로 시연할 수 있습니다.

## 7. 로컬에서 먼저 확인하기

`file://` 주소 대신 간단한 HTTP 서버를 사용하면 GitHub Pages와 유사한 방식으로 확인할 수 있습니다.

```bash
cd capstone-portfolio
python3 -m http.server 4173
```

브라우저에서 `http://localhost:4173/캡스톤디자인2포트폴리오.html#/s/s1`을 열어 학생 상세 화면과 인터랙션을 점검합니다.

> **권장 공개 방식:** 저장소는 소스 공유가 목적이면 Public으로 두고, 실제 학생 이름·연락처·개인 작업 파일이 포함될 경우에는 Public 저장소에 올리기 전에 익명화하거나 별도의 비공개 데이터 저장소를 사용하세요.

## 참고 링크

- [GitHub Pages 공식 문서](https://docs.github.com/pages)
- [Vercel 정적 사이트 배포 문서](https://vercel.com/docs/deployments)
- [Vercel Git 연동 문서](https://vercel.com/docs/git)
